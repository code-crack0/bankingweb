export async function senddebittransfer(amount, title, iban, isinstallment, duedate) {
    // await axios.post(`http://localhost:3001/api/v1/debits`, { amount: amount, title: title, iban: iban, isinstallment: isinstallment, duedate: duedate })
}